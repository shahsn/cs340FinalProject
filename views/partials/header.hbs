<header>
    Sneha Shah - <em>Welcome <span id="username">{{ username }}</span>!</em>
</header>
<nav>
    <ul>
        {{#each menuitems }}
            <li>
                <a href='{{this.location}}' {{#ifeq ../pageName this.page}}class="active"{{/ifeq}}>{{this.page}}</a>
            </li>
        {{/each}}
    </ul>
</nav>
